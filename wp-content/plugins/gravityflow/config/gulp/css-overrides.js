/**
 * Example usage to set a wrapper class of yeehaw-common for all admin components
 *
 * admin: {
 * 		components: {
 * 			wrapperClass: 'yeehaw-common',
 * 		},
 * 	}
 */

module.exports = {
	admin: {
		components: {
			wrapperClass: 'gflow-common',
		},
	}
}
